# MongoDB connection string
# Replace 'userdb' with your database name, and update the URL if necessary
MONGODB_URI=mongodb://localhost:27017/userdb

# Port for the server
# Change the port number if your local environment uses a different one
PORT=5000
